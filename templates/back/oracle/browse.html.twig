{% extends 'back/base.html.twig' %}

{% block title %}Oracles{% endblock %}

{% block body %}

<div class="menu_form">
    <div class="menu_form_buttons"><a href="{{ path('back_oracle_add') }}">Ajouter Oracle</a></div>
    <h1 class="title_form">Oracles</h1>
</div>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Image</th>
                <th>Date de creation</th>
                <th>Mis à jour le</th>
            </tr>
        </thead>
        <tbody>
        {% for oracle in oracles %}
      
            <tr>
                <td>{{oracle.id}}</td>
                <td>{{oracle.title}}</td>
                <td><img style="width:80px;" src="{{oracle.picture}}" alt=""></td>
                <td>{{oracle.createdAt | date('d-m-Y')}}</td>
                <td>{{oracle.updatedAt ?  oracle.updatedAt | date('d-m-Y') : ''}}</td>
                <td>
                    <a href="{{ path('back_oracle_read', {'id': oracle.id}) }}"><i class="fa-solid fa-glasses"></i></a>
                    <a href="{{ path('back_oracle_edit', {'id': oracle.id}) }}"><i class="fa-solid fa-pen"></i></a>
                    <a href="{{ path('back_oracle_delete', {'id': oracle.id}) }}"><i class="fa-solid fa-trash-can"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <p class="go-top"><a href="">Retour en haut <i class="fa-solid fa-arrow-turn-up"></i></a></i></p>

    
{% endblock %}